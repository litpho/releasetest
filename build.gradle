plugins {
  id 'net.researchgate.release' version '2.4.0'
}

allprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'

    sourceCompatibility = 1.8

    dependencies {
        compile 'org.slf4j:slf4j-api:1.7.21'
    }

    publishing {
        repositories {
            maven {
                url file('D:/Temp/releases')
            }
        }

        publications {
            mavenJava(MavenPublication) {
                from components.java
            }
        }
    }
}

project(':b') {
    dependencies {
        compile project(':a')
    }
}
